server {
    listen 80;
    server_name config.yourdomain.com;
    root /var/www/config;

    location /app-config.json {
        alias /var/www/config/current.json;
        add_header Cache-Control "max-age=60, must-revalidate";
        add_header Access-Control-Allow-Origin "*";
    }
}
